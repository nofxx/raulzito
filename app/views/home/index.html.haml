//- content_for :head do
//  = javascript_include_tag 'app/tabs', 'jquery/ui.core', 'jquery/ui.tabs'

- if current_user
  Hi. Welcome, <%= current_user.name %>.

  %h3 Active Matches

  %ul
    - current_user.matches.active.each do |m|
      %li= link_to m.name, match_url(m)
      %li=  '(Your move)' if m.turn_of?( current_user )


  = link_to "New Match", new_match_url
- else
  Login!

.block
  - if current_user
    #tabs
      %ul
        %li= link_to "Todas", "#fragment-1"
        %li= link_to "Notas", "#fragment-2"
        %li= link_to "Mensagens", "#fragment-3"
        %li= link_to "Análise", "#fragment-4"
      .clear
      #fragment-1
        %h2 SIG@
        .inner
          %p{:class => :first}
            = "Olá, #{current_user.name}."
            = link_to pluralize(@messages, "mensagem", "mensagens"), "/inbox", :class => "icon imail"
      #fragment-2
        %h2 SIOOO
        .inner
          %p{:class => :first}
            = "Olá, #{current_user.name}."
            = link_to pluralize(@messages, "mensagem", "mensagens"), "/inbox", :class => "icon imail"

